# src/llm_core/src/utils/token_counter.py
# Placeholder for token counter utility

class TokenCounter:
    def count_tokens(self, text: str) -> int:
        return len(text.split()) # Simple word count mock
